# TODO

Цель: довести приложение до состояния, в котором его не стыдно положить в
портфолио и можно попробовать продавать в RuStore по подписке с триалом.

Продуктовая идея — минимум трения: пользователь отмечает галочкой выполненный
подход, а веса, число подходов и разминка подобраны автоматически. В обычном
режиме руками вводятся только повторы.

## Блокирует релиз

- Сервер нигде не развёрнут: адрес задаётся при сборке через `VITE_API_URL`, и сейчас это машина в локальной сети. Нужен российский хостинг с HTTPS, иначе в релизной сборке синка нет.
- Нет подписки и гейта — продавать пока нечего.
- История тренировок со старого y-websocket-сервера (комната `user/Ne4CDW5bDDaaRuQOGSNd2ojR6Ut1`) в новую базу не перенесена.

## Продукт

- Онбординг: собрать инвентарь (вес грифа, набор блинов, шаг гантелей, стеки тренажёров). Без этого не работают ни рекомендации весов, ни калькулятор блинов. Сейчас это спрятано в настройках, и это главное трение.
- Холодный старт рекомендаций: у нового пользователя нет истории, а первая тренировка решает, останется ли он. Выбрать подход — спросить уровень и агрессивно догонять веса первые недели, либо калибровочная первая тренировка.
- Готовые программы тренировок. Голый трекер бесплатных аналогов полно, программы — то, за что платят.
- Разделение free/paid: бесплатно ведение тренировок и история, платно рекомендации, периодизация и аналитика.
- Экспорт и импорт данных.

## Рекомендации

- Написать тесты на текущее поведение `simple/` и `periodization/` до переделки — чтобы при переписывании было видно, что именно меняется в цифрах.
- Переписать. Сейчас это слои наложенных друг на друга гипотез. Интерфейс `buildRecommendations(params) => RecSetData[]` менять не нужно, он нормальный.

## Платформа

- Вход через VK ID (OAuth 2.1 + PKCE, редирект `vk{client_id}://vk.ru/blank.html` ловится через `appUrlOpen`). Аккаунт остаётся необязательным: приложение работает сразу после установки на локальном ID, логин нужен только чтобы связать устройства и подписку. Требует регистрации приложения в VK ID с SHA-1 подписи и верификации бизнес-профиля в течение 60 дней, иначе приложение блокируют.
- Деплой `src/server/` на российский хостинг: Postgres с бэкапами, HTTPS, сборка клиента с боевым `VITE_API_URL`.
- Локальные уведомления: сигнал таймера при заблокированном экране и напоминание о пропущенной тренировке. Именно локальные — серверные пуши не нужны и тянут за собой проблемы с FCM.
- RuStore: биллинг, триал, проверка чека на сервере, кэш статуса подписки с грейс-периодом на оффлайн.
- Публикация: иконки, скриншоты, описание, политика конфиденциальности. Проверить актуальные требования RuStore к аккаунту разработчика.

## Техдолг

- Производительность запросов. `queryCollection` делает полный скан коллекции, а `useQueryCollection` подписан на всю коллекцию целиком — любая запись пересчитывает все активные запросы. Лечится индексами в сторе и инкрементальным обновлением по `event.changes.keys`, менять хранилище не нужно.
- `ModalStack`: `popstate`-слушатели не снимаются при размонтировании, условие сравнения по `location.state` хрупкое при быстром двойном «назад». Концепцию (модалка = запись в history) оставить.
- Yjs не трогать до появления платящих пользователей. Апдейты схлопываются в одну строку каждые 100 записей, но история внутри документа всё равно растёт монотонно — позже понадобятся снапшоты и её обрезка.
- README описывает приложение как инструмент совместной работы в реальном времени. Это одиночное приложение, переписать.
- Проверить размер release-сборки с R8. В debug основную массу APK занимает неминифицированный dex.
