// The distance between two ticks, in pixels. It is the one measurement the
// scale cannot take from its container: it also sets how far a centimetre is
// from the next one under the thumb.
export const TICK_WIDTH = 12;
